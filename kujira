#!/usr/bin/python

import sys, os
import subprocess


if len(sys.argv) > 1:
    cmd = 'sudo mv {0} $HOME/ctf-kujira/volume/'.format(sys.argv[1])
    os.system(cmd)

os.system('sudo docker-compose run ctf-kujira zsh')

